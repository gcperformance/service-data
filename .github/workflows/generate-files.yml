name: Process and Release Service Data

on:
  push:
    branches:
      - master  # Run on all pushes to master
  pull_request:
    branches:
      - master  # Run on all PRs targeting master
  schedule:
    - cron: '0 0 * * *'  # Also keep daily runs at midnight
  workflow_dispatch:  # Allow manual triggers

jobs:
  generate-files:
    runs-on: ubuntu-latest
    steps:
      # Check out the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install requirements for python
        run: pip install -r requirements.txt

      # Ensure we have latest master
      - name: Get latest master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git fetch origin master
          git checkout master
          git pull origin master

      # WHERE AM I
      - name: Print current working directory
        run: pwd

      # Run the main.py script
      - name: Run the main.py script
        run: python main.py

      # Get current date and time for release tag
      - name: Get current date and time
        id: datetime
        run: |
          # Install tzdata for timezone support
          sudo apt-get install -y tzdata
          # Set timezone to Eastern
          export TZ="America/Montreal"
          echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT
          echo "time=$(date +'%H-%M-%S')" >> $GITHUB_OUTPUT
          echo "commit_hash=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      # Create GitHub release with SQLite database
      - name: Create SQLite Release
        id: create_sqlite_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: sqlite-${{ steps.datetime.outputs.commit_hash }}
          name: SQLite Database - ${{ steps.datetime.outputs.date }} (${{ steps.datetime.outputs.commit_hash }})
          body: |
            Automated service data SQLite release for ${{ steps.datetime.outputs.date }}
            
            Release Information:
            - Generated on: ${{ steps.datetime.outputs.date }} at ${{ steps.datetime.outputs.time }} EST
            - Git Commit: ${{ steps.datetime.outputs.commit_hash }}
            
            This release contains:
            - SQLite database with all processed data
            - Database tables are prefixed with their source directory
          files: outputs/service_data.db
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Create temporary directory for flattened structure
      - name: Prepare Flattened Files
        run: |
          mkdir flattened_files
          cd flattened_files
          find .. -type f -name '*.csv' ! -path '../flattened_files/*' -exec cp {} . \;

      # Create GitHub release with flattened CSV files
      - name: Create CSV Files Release
        id: create_csv_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: csv-${{ steps.datetime.outputs.commit_hash }}
          name: Flattened CSV Files - ${{ steps.datetime.outputs.date }} (${{ steps.datetime.outputs.commit_hash }})
          body: |
            Automated flattened CSV files release for ${{ steps.datetime.outputs.date }}
            
            Release Information:
            - Generated on: ${{ steps.datetime.outputs.date }} at ${{ steps.datetime.outputs.time }} EST
            - Git Commit: ${{ steps.datetime.outputs.commit_hash }}
            
            This release contains:
            - All CSV files from the repository in a flattened structure
          files: flattened_files/*
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Cleanup
      - name: Cleanup
        run: rm -rf flattened_files/
